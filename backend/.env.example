# VOXELINK Backend 环境变量配置示例
# 复制此文件为 .env 并填入真实的配置值

# ==================== 数据库配置 ====================
# PostgreSQL 数据库配置（必需）
POSTGRE_SERVER=localhost
POSTGRE_PORT=5432
POSTGRE_USER=your_postgres_user
POSTGRE_PASSWORD=your_postgres_password
POSTGRE_DATABASE=voxelink_db

# Redis 配置（必需）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password
REDIS_DB=0

# 数据库引擎配置（必需）
DB_ENGINE=postgresql
DB_USE_TZ=true
DB_TIME_ZONE=Asia/Shanghai

# ==================== 认证和安全 ====================
# JWT 密钥配置（必需）
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# ==================== 第三方服务 ====================
# 阿里云短信验证码配置（可选）
ALIBABA_CLOUD_ACCESSKEY_ID=your-access-key-id
ALIBABA_CLOUD_ACCESSKEY_SECRET=your-access-key-secret
ALIBABA_CLOUD_SIGN_NAME=your-sign-name

# ==================== AI 模型配置 ====================
# OpenAI API 配置（必需）
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_BASE_URL=https://api.openai.com/v1

# Anthropic API 配置（可选）
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# 自定义端点配置（可选）
CUSTOM_ENDPOINT_API_KEY=your-custom-endpoint-api-key

# ==================== 用户配置 ====================
# 用户积分配置（必需）
USER_INIT_POINTS=1000
USER_INVITE_POINTS=500

# ==================== 语音服务配置 ====================
# STT（语音转文字）配置（必需）
STT_ACTIVE_SERVICE=openai
STT_OPENAI_MODEL=whisper-1

# TTS（文字转语音）配置（必需）
TTS_ACTIVE_SERVICE=edge
TTS_EDGE_VOICE=zh-CN-XiaoxiaoNeural

# ==================== LLM 配置 ====================
# LLM 默认模型配置（必需）
LLM_DEFAULT_MODEL=gpt-3.5-turbo

# OpenAI 模型列表（必需，用逗号分隔）
LLM_OPENAI_MODELS=gpt-3.5-turbo,gpt-4,gpt-4-turbo

# Anthropic 模型列表（可选，用逗号分隔）
LLM_ANTHROPIC_MODELS=claude-3-sonnet,claude-3-haiku

# Ollama 配置（可选）
LLM_OLLAMA_BASE_URL=http://localhost:11434
LLM_OLLAMA_MODELS=llama2,codellama

# 自定义端点配置（可选）
LLM_CUSTOM_ENDPOINT_BASE_URL=http://your-custom-endpoint
LLM_CUSTOM_ENDPOINT_MODELS=custom-model-1,custom-model-2

# ==================== Redis 键名配置 ====================
# Redis 键名前缀配置（必需）
REDIS_USER_REGISTER_CODE=user:register:code:
REDIS_USER_LOGIN_CODE=user:login:code:
REDIS_USER_RESET_CODE=user:reset:code:


# ==================== TTS配置 ====================
TTS_BASE_URL="http://localhost:9880"